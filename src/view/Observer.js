/**
 * @class After a certain is action of the `controller` is finished,
 * the `Observer` takes a look at the persistent files, determines
 * what has changed and returns a `string` that will be the answer.
 */
export class Observer {

}